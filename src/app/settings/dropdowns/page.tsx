"use client";
import { useEffect, useState } from "react";
const types = [{ value: "fonte_contato", label: "Fonte" }, { value: "follow_up", label: "Follow-up" }, { value: "temperatura", label: "Temperatura" }, { value: "status_pagamento", label: "Status de pagamento" }];
export default function DropdownsPage() { const [items, setItems] = useState<Array<{ id: string; tipo: string; valor: string; cor: string }>>([]); const [valor, setValor] = useState(""); const [tipo, setTipo] = useState("fonte_contato"); const [cor, setCor] = useState("#2563eb"); useEffect(() => { fetch("/api/settings/dropdowns").then((r) => r.ok ? r.json() : []).then(setItems); }, []); async function add() { const r = await fetch("/api/settings/dropdowns", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ tipo, valor, cor }) }); if (r.ok) { setItems([...items, await r.json()]); setValor(""); } } return <main className="min-h-screen bg-slate-50 p-6 lg:p-12"><div className="mx-auto max-w-3xl"><a href="/" className="text-sm text-blue-600">← Voltar</a><h1 className="mt-8 text-3xl font-semibold">Dropdowns customizáveis</h1><p className="mt-2 text-slate-500">Defina valores e cores exibidos nos campos do pipeline.</p><div className="mt-8 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-[1fr_1fr_auto_auto]"><select value={tipo} onChange={(e) => setTipo(e.target.value)} className="rounded-xl border border-slate-200 px-3">{types.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select><input value={valor} onChange={(e) => setValor(e.target.value)} placeholder="Novo valor" className="rounded-xl border border-slate-200 px-4" /><input aria-label="Cor" type="color" value={cor} onChange={(e) => setCor(e.target.value)} className="h-11 w-14 rounded-xl border border-slate-200 p-1" /><button onClick={add} disabled={!valor.trim()} className="rounded-xl bg-blue-600 px-5 font-semibold text-white disabled:opacity-50">Adicionar</button></div><div className="mt-5 space-y-2">{items.map((item) => <div key={item.id} className="flex items-center justify-between rounded-xl border border-slate-200 bg-white p-4"><span className="flex items-center gap-3"><span className="size-3 rounded-full" style={{ backgroundColor: item.cor }} />{item.valor}</span><span className="text-sm text-slate-400">{item.tipo}</span></div>)}</div></div></main>; }
