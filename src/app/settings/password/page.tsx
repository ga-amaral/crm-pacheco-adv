"use client";
import { FormEvent, useState } from "react";
export default function PasswordPage() { const [message, setMessage] = useState(""); const [error, setError] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setError(""); setMessage(""); const form = new FormData(e.currentTarget); const currentPassword = String(form.get("currentPassword")); const newPassword = String(form.get("newPassword")); const confirmPassword = String(form.get("confirmPassword")); if (newPassword !== confirmPassword) { setError("As senhas novas não conferem."); return; } const response = await fetch("/api/auth/password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword, newPassword }) }); const data = await response.json(); if (response.ok) { setMessage("Senha alterada com sucesso."); } else { setError(data.error ?? "Não foi possível alterar a senha."); } } return <main className="min-h-screen bg-slate-50 p-6 lg:p-12"><div className="mx-auto max-w-xl rounded-2xl border border-slate-200 bg-white p-8"><a href="/" className="text-sm text-blue-600">← Voltar</a><h1 className="mt-8 text-2xl font-semibold">Alterar senha</h1><form onSubmit={submit} className="mt-8 space-y-4"><Field name="currentPassword" label="Senha atual" /><Field name="newPassword" label="Nova senha" /><Field name="confirmPassword" label="Confirmar nova senha" />{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</p>}{message && <p className="rounded-lg bg-green-50 p-3 text-sm text-green-600">{message}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Salvar nova senha</button></form></div></main>; }
function Field({ name, label }: { name: string; label: string }) { return <label className="block text-sm font-medium">{label}<input name={name} type="password" required minLength={8} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-blue-500" /></label>; }
