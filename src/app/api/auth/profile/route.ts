import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";

export async function GET() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Não autenticado" }, { status: 401 }); const { data, error } = await supabase.from("users").select("id,nome,email,role,ativo").eq("id", user.id).single(); if (error) return NextResponse.json({ error: error.message }, { status: 500 }); return NextResponse.json(data); }
export async function PATCH(request: Request) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Não autenticado" }, { status: 401 }); const body = await request.json().catch(() => ({})); const update: { nome?: string } = {}; if (body.nome !== undefined) { if (typeof body.nome !== "string" || body.nome.trim().length < 2) return NextResponse.json({ error: "O nome deve ter pelo menos 2 caracteres." }, { status: 400 }); update.nome = body.nome.trim(); } if (body.newPassword !== undefined && (typeof body.newPassword !== "string" || body.newPassword.length < 8)) return NextResponse.json({ error: "A nova senha deve ter pelo menos 8 caracteres." }, { status: 400 }); const { data, error } = await supabase.from("users").update(update).eq("id", user.id).select("id,nome,email,role,ativo").single(); if (error) return NextResponse.json({ error: error.message }, { status: 500 }); if (body.newPassword !== undefined) { const passwordResult = await supabase.auth.updateUser({ password: body.newPassword }); if (passwordResult.error) return NextResponse.json({ error: passwordResult.error.message }, { status: 500 }); } return NextResponse.json(data); }
